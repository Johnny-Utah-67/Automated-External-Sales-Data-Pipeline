🎯 Goal
Preserve raw, immutable files for traceability and reprocessing.

```Python
blob_client.upload_blob(csv_buffer.getvalue(), overwrite=True)Show more lines
What this shows
```

Cloud object storage as a landing zone
Clear separation between raw and curated data
Ability to reprocess history if needed
